# 2026-09-17

## Orientation

Read `memory/INDEX.md`, `memory/ops.md`, `memory/repos/ivy.md`,
`memory/repos/tomgreen.ai.md`, `memory/repos/talent-radar.md`,
`memory/repos/c2-client-matrix.md`, and `memory/patterns.md` before pulling
any live data — standing context: cloud sandbox egress is repo-scoped to
`ivy` (`check.sh` exits 2, expected); the local-WIP scanner has been dark
since 2026-09-08; `tomgreen.ai` PR #59 has been the unconverted top pick for
seven straight days; `talent-radar` PR #3 is blocked on 3 P1 findings + red
CI; `c2-client-matrix` stays parked.

## Watchlist sync

`search_repositories org:tompulsarlabs` — **21 repos, 0 forks, 0 archived,
0 excludes.** Same 21 names as the 09-16 sync; no change to `config.yml`
needed.

## Attribution check

`search_commits org:tompulsarlabs author-date:2026-09-17` and
`search_pull_requests org:tompulsarlabs created:2026-09-17` are both empty
this morning — nothing to check yet today. No same-morning attribution
nudge needed on GitHub-side history. Local checkouts are a separate
question and still unknown — see `## Local WIP`.

## Open PRs (org-wide, 18 total — unchanged count and set from yesterday)

`search_pull_requests org:tompulsarlabs is:open` returned the same 18 PRs
as 09-16.

1. **`talent-radar` #2** — "Build Radar's private executive beta with
   grounded interview evaluation" (draft). **`updated_at` moved to
   2026-09-16T12:40:58Z**, the first change since it was opened 09-06 and
   last reviewed 09-05 (`2026-09-05-talentradar-review-01`, verified done
   via PR-body corroboration, before this update). `search_commits
   repo:tompulsarlabs/talent-radar author-date:2026-09-11..2026-09-17`
   returns zero — but `search_commits` only indexes the default branch
   [[ops]], so it cannot confirm or rule out new commits on this draft's
   own branch; the `updated_at` move could be a body edit, a comment, or
   real new commits, and this session cannot tell which. Per the
   playbook's revealed-focus rule, a draft PR Tom updated this week ranks
   above a stale untouched one regardless — **today's top pick**, see
   `## Dispatch queue` for the fresh review contract queued against it.
2. **`tomgreen.ai` #59** — "Reserve original product materials under
   proprietary licence" (open, not draft, opened 09-10, unchanged).
   Mechanical, low-risk licence-text change completing the 09-10 sweep;
   ready for Tom's merge decision. **Eighth consecutive day unconverted**,
   now standing behind PR #2's fresher signal — see `## Top pick`.
3. **`tomgreen.ai` #58** — "Sharpen Radar demo subtitle" (draft, opened
   09-10, unchanged). Reviewed and verified done
   (`2026-09-11-tomgreenai-radarsubtitle-review-01`); awaiting Tom's merge
   decision, not new scout work.
4. **`tomgreen.ai` #34** — "Fix Calendly capitalization in contact orbit."
   Open, not draft, unchanged since 09-06 — now **11 days stale**, ready
   to merge outright.
5. **`talent-radar` #3** — no new commits confirmed since 09-11 (same
   `search_commits` caveat as PR #2 above — default-branch-only, so this
   is corroboration not proof). Stays blocked on the three P1 findings +
   red CI from `2026-09-09-talentradar-sybilintake-review-01` (verified
   done) [[repos/talent-radar]]. Now **9th day** blocked without a build
   pass.
6. **`pixel-perfect-showcase-8458` #1/#2**, **`talent-radar` #1**, **`ivy`
   #18/#19/#20**, **`tomgreen.ai` #13/#14/#15**, **`countersign` #1** —
   all unchanged since prior days; review contracts already verified
   done. Awaiting human merge/review decisions, not new scout work.
7. **`c2-client-matrix` #1** — parked: no candidate, contract, or audit
   produced [[repos/c2-client-matrix]].
8. **`BrightPaws` #1**, **`ai-capability-app` #4** — draft, stale since
   05-03/04-26. Known non-movers.

No new open issues (`search_issues org:tompulsarlabs is:open` returns 0 —
the per-repo `open_issues_count` GitHub reports elsewhere counts PRs as
issues, a known quirk, not real issue activity).

## Local WIP

**Still unknown, now further overdue.** `local-wip.json` has not
refreshed since 2026-09-08T15:45:06Z — see `## Blockers`, now **9 calendar
days / 17 missed windows**, including this morning's 08:45 CEST slot. Last
known state (09-08 scan) remains what's on record: `talent-radar` had 2
unpushed commits / 4 dirty files on `main-wre2w1`; all other nine scanned
repos clean. Not treated as current fact today — a sleeping Mac must not
read as a clean tree.

## Blockers

**Local-WIP scanner has now missed 17 consecutive windows and persisted 9
calendar days.** `local-wip.json` last landed 2026-09-08T15:45:06Z. This
crossed the playbook's 3-day outranking bar on 09-11 and is now six days
further past it with no recovery signal. "push X (N unpushed commits)"
stays invisible to every routine while it persists [[repos/ivy]]. Smallest
next action unchanged: confirm the Mac's `launchd` job for
`scripts/local-wip.sh` is loaded and the machine has been awake through
the missed windows. Carrying forward until a fresh `local-wip.json` lands
or Tom explicitly declines to fix it.

**New data point, possibly related:** `dispatch/runner-status.json`'s
`last_tick` timestamps shifted from `+02:00` (CEST) to `+01:00` on
2026-09-16 (`09:29:42+01:00`, `15:39:58+01:00`) — a full day before the
real CEST→CET flip this playbook's DST note expects in late October. That
reads as the Mac's system clock or timezone changing, not a code change
(the runner doesn't set its own offset). Worth checking whether this
coincides with whatever stopped `local-wip.sh`'s launchd job — same
machine, same window. Not treated as confirmed causation, just flagged
alongside the scanner outage for whoever investigates. `dispatch/queue/`
is still empty, so per the playbook's test this remains **not** a runner
blocker in its own right (nothing open for a stalled runner to be
blocking) — but a clock skew on the box that also runs dispatch workers is
worth a look regardless.

## Dispatch queue

Empty going into today. Emitting one new contract (see below); 1 of
`dispatch.daily_cap` (6) used.

**`2026-09-17-talentradar-execbeta-review-02`** — fresh review of
`talent-radar` PR #2 at its current head, since `updated_at` moved
2026-09-16 and the last review (`2026-09-05-talentradar-review-01`)
predates that change. Lane `workhorse`, pool `openai` (non-author family,
default per the 09-06 retro pin, same pairing as the three prior
`talent-radar` review contracts). Queued via `dispatch/queue/`,
`scripts/dispatch-lint.sh` clean.

## Top pick

**`talent-radar` PR #2 — draft Tom updated 2026-09-16, the freshest
revealed-focus signal this week.** Outranks the standing `tomgreen.ai`
PR #59 (eighth consecutive unconverted day) per the playbook's rule that a
draft PR updated this week ranks above a stale untouched one. A fresh
review contract is queued against its current head (`##
Dispatch queue`) since the last review predates the update and this
session cannot confirm from `search_commits` alone whether the change was
substantive. Standing behind it: `tomgreen.ai` #59 (validated, mechanical,
day 8), #58 (reviewed, awaiting merge), #34 (cheap, now 11 days stale),
`talent-radar` #3 (blocked on 3 P1 findings + red CI, day 9). **Still
worth flagging plainly: the local-WIP scanner outage (`## Blockers`) is
more overdue than any open candidate** — 9 calendar days / 17 missed
windows, six days past the playbook's 3-day bar with no sign of clearing.
It is not this entry's top *candidate* only because a dead scanner
produces no shippable content of its own; if today's 18:00 check is still
grey, it remains the correct nudge per the playbook's blocker-outranking
rule, same as the prior five days.
