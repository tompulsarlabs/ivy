# 2026-09-24

## Orientation

Read `memory/INDEX.md`, `memory/ops.md`, `memory/repos/ivy.md`,
`memory/repos/talent-radar.md`, and `memory/repos/talent-scout.md` before
pulling live data — standing context: cloud sandbox egress is repo-scoped to
`ivy` (`check.sh` exits 2, expected); the local-WIP scanner has been dark
since 2026-09-08 (15 calendar days as of yesterday), with a fix (`ivy` PR
#22) opened last night but not yet merged or run; `talent-radar` PR #2's
09-21 head was reviewed 09-23 (P1 CSV-dedupe finding still open, unfixed
across two review cycles); `talent-scout` PR #2 was reviewed 09-23 (no
actionable findings, verified done); `tomgreen.ai` PR #59 is the standing
mechanical pick, now 15 straight days unconverted; `talent-radar` #3 stays
blocked on 3 P1 findings + red CI; `c2-client-matrix` stays parked.

## Watchlist sync

`search_repositories org:tompulsarlabs` — **22 repos, 0 forks, 0 archived.**
Exact match against `config.yml`'s current 22-repo list — no additions, no
drops. No commit needed.

## Attribution check

`search_commits org:tompulsarlabs author-date:2026-09-24` is empty this
morning (07:10 UTC / 09:10 CEST) — nothing to check yet today. No
same-morning attribution nudge needed on GitHub-side history. Local
checkouts remain a separate, unknown question — see `## Local WIP`.

## Open PRs (org-wide, 23 total — three more than 09-23's 20)

`search_pull_requests org:tompulsarlabs is:open` shows the same 20 PRs as
09-23 plus the three opened after last night's failsafe check: `ivy` #21,
#22, #23. No new PR opened overnight beyond those three, no further
`updated_at` movement on anything else.

1. **`ivy` #22** — "Restore local work visibility and isolate scanner
   publication" (draft, opened 2026-09-23T18:39:29Z, head `173a9e6`,
   `mergeable_state: unknown`, no CI status yet). This is the fix for the
   standing local-WIP scanner blocker (`## Blockers`) — worktree discovery,
   a per-checkout/repo-wide unpushed count, a six-hour unchanged-heartbeat,
   and bot-authored snapshot publication from a temporary clone instead of
   the operator's own checkout. Body claims 23 deterministic scanner
   controls pass plus a read-only Mac scan of 20 checkouts. `launchd`
   remains unloaded and no live snapshot has published — a fix landing,
   not the outage resolving. **No review contract existed yet — emitted
   one today** (see `## Dispatch queue`).
2. **`ivy` #23** — "Apply harness effort settings and record execution
   provenance" (draft, opened 2026-09-23T18:52:27Z, head `e23037c`,
   `mergeable_state: blocked`, no CI status yet). Body claims the dispatch
   runner silently dropped each lane's configured `effort`, so frontier and
   workhorse produced identical launch commands; this PR wires effort
   through to the CLI, rejects unknown model/harness/effort combos before
   claim, and adds execution provenance. **No review contract existed
   yet — emitted one today.**
3. **`ivy` #21** — "Re-tune the playbook, routine prompts, and dispatch for
   the current models; add a routine eval" (draft, opened
   2026-09-23T18:09:42Z, updated 2026-09-23T21:24:05Z, head `5937264`,
   `mergeable_state: blocked`). The largest and highest-consequence PR
   queued today: rewrites `playbook.md`'s Tunable sections and all four
   routine prompts, claims every Immutable section stays byte-identical,
   and reports an eval (25 replayed-day cases) showing 45/46 preserve-runs
   and 4/4 change-runs passing on the candidate vs. 35/46 and 0/4 on
   baseline. The PR body itself flags a direct file-level conflict with
   #23 (`scripts/dispatch-runner.py`, `config.yml` lane-effort wiring) and
   recommends merging #23 first — a merge-order call for Tom, not
   something this scout can resolve. **No review contract existed yet —
   emitted one today, frontier lane** given the stakes (a wrong Tunable
   rewrite silently changes every subsequent run).
4. **`talent-scout` #2** — reviewed and verified done 09-23
   (`2026-09-23-talentscout-workspace-review-01`, no actionable findings).
   Unchanged since 2026-09-22T06:43:03Z.
5. **`talent-radar` #2** — reviewed and verified done 09-23
   (`2026-09-23-talentradar-execbeta-review-03`): one open P1 (CSV dedupe
   at `src/lib/market/import.ts:44`, unfixed across two review cycles now)
   plus a new P3. Unchanged since 2026-09-21T16:51:46Z.
6. **`tomgreen.ai` #59** — "Reserve original product materials under
   proprietary licence" (open, not draft, unchanged since 09-10).
   Mechanical, validated, ready for Tom's merge decision. **Fifteenth
   consecutive day unconverted.**
7. **`tomgreen.ai` #58** — reviewed and verified done, awaiting merge
   decision, unchanged.
8. **`tomgreen.ai` #34** — "Fix Calendly capitalization in contact orbit."
   Nudged yesterday (18:00 check), did not convert — the day went green on
   the three `ivy` PRs instead. Open, not draft, unchanged since 09-06 —
   now **18 days stale**.
9. **`talent-radar` #3** — stays blocked on the three P1 findings + red CI
   from `2026-09-09-talentradar-sybilintake-review-01`. Now **16th day**
   blocked without a build pass.
10. **`pixel-perfect-showcase-8458` #1/#2**, **`talent-radar` #1**, **`ivy`
    #18/#19/#20**, **`tomgreen.ai` #13/#14/#15**, **`countersign` #1** —
    all unchanged since prior days; review contracts already verified done.
    `ivy` #20 (Paperclip acceptance stack) did gain an `updated_at` bump to
    2026-09-23T18:54:53Z but reading the full body shows it only records
    that #22/#23 exist as separate operational-repair branches — no code
    change here, no new candidate.
11. **`c2-client-matrix` #1** — parked: no candidate, contract, or audit
    produced.
12. **`BrightPaws` #1**, **`ai-capability-app` #4** — draft, stale since
    05-03/04-26. Known non-movers.

No open issues (`search_issues org:tompulsarlabs assignee:tompulsarlabs
is:open` returns 0, same known quirk as prior days).

## Local WIP

**Still unknown, now further overdue.** `local-wip.json` has not refreshed
since 2026-09-08T15:45:06Z — now **31 missed windows / 16 calendar days**,
thirteen days past the playbook's 3-day outranking bar. Not treated as
current fact today — a sleeping Mac must not read as a clean tree. The fix
(`ivy` PR #22) is queued for review today; per its own body, `launchd`
remains unloaded and no live snapshot has published yet, so this line
carries forward unchanged in substance even though the blocker's fix is
now visible and being reviewed.

## Blockers

**Local-WIP scanner has now missed 31 consecutive windows and persisted 16
calendar days**, thirteen days past the playbook's 3-day outranking bar.
"push X (N unpushed commits)" stays invisible to every routine while it
persists [[repos/ivy]]. Smallest next action has changed for the first
time in two weeks: a fix (`ivy` PR #22) is open and queued for review
today rather than "confirm the Mac's launchd job is loaded" — reviewing
and merging #22, then running its post-merge steps (install, one scan,
verify the remote snapshot), is now the concrete path to closing this
blocker. Carrying forward until a fresh `local-wip.json` lands or Tom
explicitly declines to fix it.

`dispatch/runner-status.json`'s `last_tick` is `2026-09-23T19:11:15+02:00`
(inside yesterday's 09:15-21:00 window) — result `idle`, `harness` and
`lint_ok` both true, nothing `skipped`. Today's window hasn't opened yet at
scout time (09:10 CEST), so this isn't stale by the literal 3-hour test.

**Separately worth naming, not a blocker in the outranking sense:** `ivy`
PR #21's body states a direct file-level conflict with PR #23 on
`scripts/dispatch-runner.py` and `config.yml`'s lane-effort wiring, and
recommends merging #23 first. This is a merge-order decision only Tom can
make — queued today's review contracts will assess each PR on its own
merits, but neither review nor this scout can resolve which merges first.

## Dispatch queue

Empty going into today. **Three new contracts emitted**, all against `ivy`
itself, all opened as draft PRs last night with no review contract yet:

- `2026-09-24-ivy-scannerfix-review-01` (`review`, `lane: workhorse`,
  `pool: anthropic` — PR #22 is codex-authored, pinning the non-author
  family) against PR #22, the scanner-outage fix.
- `2026-09-24-ivy-harnesseffort-review-01` (`review`, `lane: workhorse`,
  `pool: anthropic`, same pin logic) against PR #23, the lane-effort wiring
  fix, with an explicit note to record the #21 merge-order conflict.
- `2026-09-24-ivy-playbookretune-review-01` (`review`, `lane: frontier`,
  `pool: openai` — PR #21 is claude-authored, pinning the non-author
  family) against PR #21, the playbook/routine re-tune. Frontier over
  workhorse: this is the highest-consequence review queued today, since a
  wrong Tunable rewrite changes every subsequent scout/check/failsafe run
  silently.

Ran `scripts/dispatch-lint.sh` — ok, 32 contracts, schema and cap hold. 3 of
`dispatch.daily_cap` (6) used.

## Top pick

**`ivy` PR #22 — review, then merge to clear the local-WIP scanner
blocker.** Of today's three fresh `ivy` candidates, #22 is the one whose
value compounds: it is the actual fix for the outage that has now stood
16 calendar days past the playbook's 3-day bar, and closing it restores
"push X (N unpushed commits)" as a visible candidate class for every future
scout run — the same class that supplied the day's top pick on
2026-08-25/26 before the scanner went dark. #21 (playbook re-tune) is more
consequential in scope but also the largest and slowest to review safely
(95 files, an Immutable-section claim that must be checked byte-for-byte);
#23 (harness effort) is smaller but its own merge order is contested by
#21's body. All three are queued for review today regardless.

Standing behind the `ivy` trio: `tomgreen.ai` #59 (validated, mechanical,
day 15 unconverted), #58 (reviewed, awaiting merge), #34 (cheap, now 18
days stale, unconverted from yesterday's nudge), `talent-radar` #3
(blocked on 3 P1 findings + red CI, day 16).

**Still worth flagging plainly: the local-WIP scanner outage (`##
Blockers`) is more overdue than any open candidate** — 16 calendar days /
31 missed windows, thirteen days past the playbook's 3-day bar. Its fix is
now in review rather than still unstarted, which is real movement, but the
outage itself has not yet cleared.

## Verification

18:03 CEST check (bot-authored, silent per `quiet_when_green`) saw 3
connected-author events by then: `ivy` PR #22 and #23 both merged
08:42 CEST, `tomgreen.ai` PR #66 merged 10:44 CEST.

22:30 CEST failsafe re-run of `list_commits`(ivy) + `search_commits` +
`search_pull_requests`(created/merged) + `search_issues`(created), all
`org:tompulsarlabs` `2026-09-24`, found 5 further connected-author events
since the check, all `tom@pulsarlabsai.com` / resolves `author.login
tompulsarlabs`, default branch, non-fork, OWNER association:

- `talent-radar` PR #1 merged 17:24:31 CEST — the Cowork-history restore +
  Session 1 Supabase fetch layer + Radar UI build, open since 09-03,
  finally landed on `main` via a merge commit that preserves its
  individual commits (per the PR's own body) — explains why
  `search_commits` also surfaces a run of `talent-radar` docs/feat commits
  timestamped 10:53–13:19 CEST: they were authored on the PR branch and
  only became default-branch-reachable at the 17:24 merge.
- `talent-radar` PR #2 merged 17:24:29 CEST — the private executive beta,
  open since 09-06, reviewed twice ([[repos/talent-radar]]).
- `tomgreen.ai` PR #67 merged 17:34:52 CEST — `/lab` made the canonical
  route.
- `tomgreen.ai` PR #68 merged 19:39:41 CEST — Substack essay added to Lab.
- `talent-radar` PR #4 opened 19:19:30 CEST, draft — records the beta's
  production promotion; still open at 22:30, next candidate for tomorrow's
  scout if it stays open.

No further connected-author activity found after 19:39:41 CEST through
the 22:30 re-check. Zero issues opened. Today closes as real-work green
across three repos (`ivy`, `tomgreen.ai`, `talent-radar`), the widest
spread since 09-10.

**Local-WIP scanner: first scheduled-window publish since the outage
began.** `local-wip.json` `generated_at` is now `2026-09-24T15:45:07Z`
(17:45:07 CEST) — inside today's scheduled 17:45 window, not a manual
run. This is the first snapshot to land inside a scheduled window since
2026-09-08, 16 calendar days / 32 missed windows ago. An earlier snapshot
today (08:43:47Z / 10:43 CEST, `ivy` commit `1c6da31`) fell in neither
scheduled window and was read by the `scannerfix` review report as a
manual run made possible by PR #22's merge, with `launchd` itself still
unloaded at review time. Whether the 17:45 publish means `launchd` is now
loaded, or is a second manual run that happens to coincide with the
window, cannot be told from the cloud side — but two publishes in one day
after 16 dark days is the strongest signal yet that the outage is
clearing. See `## Blockers` and [[repos/ivy]] for the carry-forward
decision.

`dispatch/runner-status.json` `last_tick` is `2026-09-24T17:33:15+02:00`,
inside today's runner window, `harness` and `lint_ok` both true, `skipped`
empty — no blocker.

## Dispatch verification

Three contracts in `dispatch/done/` carried no `verified:` stamp going
into tonight — all opened and completed today, all against `ivy` itself:

- `2026-09-24-ivy-scannerfix-review-01` — report exists on `main`
  (`c2e80d5`, confirmed matching `origin/main`), non-empty, four findings
  (F1–F4) tied to file:line in `scripts/local-wip.py` /
  `setup/SETUP.md`, plus a scope note distinguishing the reviewed head
  `173a9e6` from the merged head `2524ad9`. Because this session's access
  covers `ivy` directly, the reviewed paths were spot-checked against the
  local clone rather than falling back to PR-body corroboration.
  **verified: true.**
- `2026-09-24-ivy-harnesseffort-review-01` — report exists on `main`
  (`c6a67b6`), non-empty, five confirmations plus three findings (F1–F3)
  tied to file:line in `scripts/dispatch-runner.py`. PR #23 confirmed
  merged same day (`022dc24`, 08:42:25 CEST). **verified: true.**
- `2026-09-24-ivy-playbookretune-review-01` — report exists on `main`
  (`b308367`), non-empty, verdict **block** (one P1, three P2) plus two
  confirmed claims (Immutable byte-identity, eval arithmetic). PR #21
  confirmed still open (unmerged) — consistent with the block verdict
  standing. **verified: true.** Worth carrying forward: the P1 finding
  says the PR's own rewritten cloud green-path fallback can misclassify
  an already-green day as grey when a same-day opened PR is missed by the
  search queries and PR reviews aren't checked at all — exactly the kind
  of gap this failsafe's own verification path needs to stay alert to.

All three stamped, `scripts/dispatch-lint.sh` re-run clean (32 contracts).
`dispatch/failed/` re-checked, all five entries unchanged from prior
runs, none newly past `expires` in a state needing action.
`dispatch/queue/` empty, nothing `claimed` pending.
