# 2026-09-25

## Orientation

Read `memory/INDEX.md`, `memory/ops.md`, `memory/patterns.md`,
`memory/repos/ivy.md`, and `memory/repos/talent-radar.md` before pulling
live data — standing context: cloud sandbox egress is repo-scoped to `ivy`
(`check.sh` exits 2, expected); yesterday (09-24) closed real-work green
across three repos (`ivy`, `tomgreen.ai`, `talent-radar`) — the widest
spread since 09-10 — with `ivy` PR #22/#23 merged, `tomgreen.ai` PR
#66/#67/#68 merged, and `talent-radar` PR #1/#2 merged plus PR #4 opened
(still open); the local-WIP scanner published inside a scheduled window for
the first time since 09-08 (17:45 CEST yesterday), a second such publish
needed to confirm `launchd` itself is loaded rather than a lucky manual-run
coincidence; `ivy` PR #21 (playbook/routine re-tune) was reviewed 09-24 at
head `5937264` with verdict **block** (one P1, three P2) and its body now
claims all four fixed on later commits; `talent-radar` PR #2's CSV-dedupe
finding has survived two review cycles unfixed; `c2-client-matrix` stays
parked.

## Watchlist sync

`search_repositories org:tompulsarlabs` — **22 repos, 0 forks, 0
archived.** Exact match against `config.yml`'s current 22-repo list (same
set, same order-independent membership) — no additions, no drops, no
excludes needed. No commit needed.

## Attribution check

`search_commits org:tompulsarlabs author-date:2026-09-25` is empty this
morning (07:1x UTC) — nothing to check yet today on GitHub-side history.

Local checkouts (`local-wip.json`, `generated_at: 2026-09-24T15:45:07Z`,
~15h old — fresh, well under the 36h staleness bar): 22 checkouts, all 22
`author_email_ok: true`. One `last_commit_email_ok: false` —
`ivy-paperclip-release` (branch `codex/ivy-acceptance-scaffold`, `ivy` PR
#20's head, `unpushed_commits: 0`). Checked directly rather than nudged:
`list_commits` on that branch shows the actual head
(`d2f9ad2d8062b5e3a4a946a9659a5f55f78686a4`) is `ivy-bot <bot@ivy.invalid>`
— intentional status-recording bookkeeping on a long-running internal
handoff/planning branch (PR #20's own body says to keep it draft,
unmerged), not an invented per-host identity on real work. This is the
exact pattern the 09-24 `scannerfix` review already found and left
unfixed as non-blocking: "bot-authored worktree HEADs misreport
`last_commit_email_ok: false`" ([[repos/ivy]]). No nudge — a genuine
broken-identity incident would show a human commit under a non-connected
address, not a bot-authored one; the two most recent human-authored
commits on this branch (`114b4e72`, `ff49d401`) are both correctly
`tompulsarlabs`/`tom@pulsarlabsai.com`, resolved `author.login`.

## Local WIP

`local-wip.json` refreshed inside a scheduled window for the second day
running — the scanner outage looks recovered, though a full week without a
miss would be the stronger confirmation. Two entries carry unpushed
commits worth naming:

- **`talent-radar-pilot`** (branch `codex/executive-pilot`, remote
  `tompulsarlabs/talent-radar`) — **2 unpushed commits**, `last_commit:
  2026-09-24`. The scan (`15:45:07Z` / 17:45 CEST) ran *after* PR #1 and
  PR #2 both merged (17:24 CEST) — so these 2 commits could be genuine
  further work on top of the merge, or a stale worktree pointing at a
  pre-merge state that the merge already superseded. Cannot tell from the
  cloud side which; naming it rather than guessing.
- **`Talent Radar`** (branch `main-wre2w1`, same remote) — 2 unpushed
  commits, `last_commit: 2026-09-03` — three weeks stale, likely a
  leftover worktree from the PR #1 build. Lower priority than the fresh
  one above.

Both are Tom's own local action, not something a dispatch contract can
push (no Mac credentials in the cloud sandbox).

## Open PRs (org-wide, 20 open)

`search_pull_requests org:tompulsarlabs is:open`, sorted by `updated`:

1. **`talent-radar` #4** — "Record Radar production promotion and
   remaining beta gates" (draft, opened 2026-09-24T19:19:30Z, updated
   19:20:19Z). Freshest PR in the org, direct continuation of yesterday's
   merges. **No review contract existed yet — emitted one today** (see
   `## Dispatch queue`), specifically checking whether the promotion
   record acknowledges the still-open CSV-dedupe P1 from PR #2's last
   review.
2. **`ivy` #21** — "Re-tune the playbook, routine prompts, and dispatch
   for the current models; add a routine eval" (draft, head moved from
   `5937264` (09-24 review) to `e2d21c3a1...`, 18 more commits,
   `mergeable_state: blocked`, no CI status registered). Body now claims
   all four 09-24 findings fixed, with commit shas cited for each, plus a
   second blind eval pass (47/48 preserve, 3/4 change) and point-by-point
   responses to the review including two declined proposals with stated
   reasoning. **No fresh review contract existed against the new head —
   emitted one today** to confirm the fixes actually close what was
   found, not just touch the named files, and to re-check the
   Immutable-section byte-identity claim at the new head.
3. **`tomgreen.ai` #59** — "Reserve original product materials under
   proprietary licence" (open, not draft, unchanged since 09-10).
   Mechanical, previously validated, ready for Tom's merge decision.
   Sixteenth consecutive day unconverted.
4. **`tomgreen.ai` #34** — "Fix Calendly capitalization in contact orbit."
   Open, not draft, unchanged since 09-06 — now 19 days stale. Nudged
   09-23, did not convert.
5. **`talent-radar` #3** — stays blocked on three P1 findings + red CI
   from `2026-09-09-talentradar-sybilintake-review-01`. Now 17th day
   blocked without a build pass.
6. **`talent-scout` #1/#2** — both draft, unchanged since 09-21/09-22,
   both previously reviewed and verified done (no actionable findings on
   #2). No movement to report.
7. **`ivy` #18/#19/#20**, **`tomgreen.ai` #13/#14/#15/#58**,
   **`pixel-perfect-showcase-8458` #1/#2**, **`countersign` #1**,
   **`BrightPaws` #1**, **`ai-capability-app` #4** — all unchanged since
   prior days; review contracts already verified done where applicable.
   `ivy` #20 (Paperclip acceptance stack) explicitly stays draft/unmerged
   per its own body — internal handoff document, not a shippable
   candidate.
8. **`c2-client-matrix` #1** — parked: no candidate, contract, or audit
   produced.

No open issues (`search_issues org:tompulsarlabs assignee:tompulsarlabs
is:open` returns 0, same known quirk as prior days).

## Blockers

**Local-WIP scanner: likely recovered, not yet confirmed stable.** Two
scheduled-window publishes now (yesterday's 17:45 CEST, following the
`ivy` PR #22 merge), after 16 dark calendar days. Carrying forward as a
watch item rather than a blocker: if `launchd` is genuinely reloaded, this
should keep landing at 08:45/17:45 without further intervention; the
scout will treat a missed window from here as a fresh regression, not a
continuation of the old outage.

`dispatch/runner-status.json` `last_tick` is `2026-09-24T17:33:15+02:00`
(inside yesterday's window), result `idle`, `harness` and `lint_ok` both
true, `skipped` empty. Today's window hasn't opened yet at scout time
(09:00 CEST) — not stale by the literal 3-hour test, and `dispatch/queue/`
was empty going into today, so nothing was waiting on the runner overnight.

**Separately worth naming, not a blocker in the outranking sense:** the
09-24 `scannerfix` review left four non-blocking findings unfixed in
`scripts/local-wip.py`, including the bot-authored-worktree-HEAD
misreport confirmed harmless above. No urgency change from yesterday.

## Dispatch queue

Empty going into today. **Two new contracts emitted**, both `lane:
workhorse`, `pool: openai`:

- `2026-09-25-ivy-playbookretune-review-02` against `ivy` PR #21's current
  head — confirms or refutes the four fix-claims in the PR body since the
  09-24 block-verdict review, and re-checks the Immutable byte-identity
  claim at the new head.
- `2026-09-25-talentradar-productionpromo-review-01` against `talent-radar`
  PR #4 — checks whether the production-promotion record acknowledges the
  still-open CSV-dedupe P1 from PR #2's last review, and whether the
  stated "remaining beta gates" list is complete.

Ran `scripts/dispatch-lint.sh` — ok, 34 contracts, schema and cap hold. 2
of `dispatch.daily_cap` (6) used.

## Top pick

**`talent-radar` PR #4 — review, then Tom's promotion decision.** It is
the freshest signal in the org (opened last night, still open), it
directly continues yesterday's widest-ever real-work spread, and it
carries a real risk worth surfacing before any promotion happens: whether
the production-promotion record is honest about the still-unfixed
CSV-dedupe bug in the feature it promotes. `ivy` PR #21 is close behind —
higher-consequence in scope (it rewrites every future routine's Tunable
behavior) but its own body already claims the 09-24 block-verdict is
resolved, so today's move is confirming that claim, not re-deriving it
from scratch. Standing behind both: `tomgreen.ai` #59 (validated,
mechanical, day 16 unconverted) and #34 (cheap, day 19 stale).

Local WIP adds one live, unresolved question rather than a clean pick:
`talent-radar-pilot`'s 2 unpushed commits on `codex/executive-pilot`,
dated after yesterday's PR #1/#2 merges — possibly new work, possibly a
stale pre-merge worktree. Worth a one-line check from Tom, not a nudge.
