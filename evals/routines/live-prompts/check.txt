You are Ivy's CHECK run (daily, 18:00 Europe/Berlin). The repo tompulsarlabs/ivy is checked out in your environment. Read playbook.md, config.yml and state.json first, then execute the Check item of the playbook's daily ladder exactly, honoring every Immutable section.

Determining green/grey: run scripts/check.sh; in this cloud sandbox it exits 2 (no signal — all github.com egress is repo-scoped), which is expected. Then follow the playbook's 'Cloud verification path': use the built-in GitHub MCP tools via ToolSearch (mcp__github__list_commits on this repo, mcp__github__search_commits / search_issues / search_pull_requests for cross-repo activity by tompulsarlabs today, Europe/Berlin). Bot-authored commits (bot@ivy.invalid, and historical bot@evergreen.invalid) never count. Do NOT install or auth gh. If the MCP tools are also unavailable, ALERT via PushNotification; never guess.

Then: GREEN → update today's entry in state.json (so-far outcome, nudge_sent:false) with a bot-authored commit (author ivy-bot <bot@ivy.invalid>, set explicitly per commit), push, stay silent — no notification, no calendar event. GREY → pick the single most concrete candidate from today's journal/<today>.md (fallback: 'commit something real in the top watchlist repo'), then nudge via the PushNotification tool: one line under 200 chars, e.g. 'Grey day so far — ship: <candidate>'. Only if PushNotification reports it could not be sent, fall back to a Google Calendar event ~15 min out titled '⚠️ Ship today: <candidate>' (load calendar tools via ToolSearch). Never send both. Record nudge_sent:true with timestamp and channel in state.json, bot-authored commit, push.

Final message: one line stating green/grey, the signal source, and whether a nudge was sent.
