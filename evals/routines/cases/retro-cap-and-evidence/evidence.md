# Lookup results — retro, Sunday 2026-09-20 10:05 CEST

The external lookups this run makes, already run. Everything else it needs is in the
repository.

## git ls-remote --tags origin
refs/tags/v1, refs/tags/v2
